{
  "buildCommand": "npm run build && mkdir -p dist/overview/assets && cp -r ../../overview dist/ && cp assets/icon.png dist/overview/assets/",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/overview/(.*)",
      "dest": "/overview/$1"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
