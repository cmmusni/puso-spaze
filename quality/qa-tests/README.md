# QA Test Scripts

Test scripts generated by the QA Tester agent. Re-run with:

```bash
# Individual test
npx tsx quality/qa-tests/<test-name>.ts

# All tests
for f in quality/qa-tests/*.ts; do npx tsx "$f"; done
```
