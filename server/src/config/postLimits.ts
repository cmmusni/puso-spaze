// ─────────────────────────────────────────────
// Post content length limits
// Keep in sync with: packages/core/constants.ts
// ─────────────────────────────────────────────

/** Minimum post content length */
export const POST_MIN_LENGTH = 3;

/** Maximum post content length */
export const POST_MAX_LENGTH = 1000;
